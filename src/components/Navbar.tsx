import { Nav, Logo, SearchBar, NavItems, NavItem } from "../styles";

const Navbar = () => {
  return (
    <Nav>
      {/* <Logo to="/">YouTube</Logo>
      <SearchBar type="text" placeholder="Search" />
      <NavItems>
        <NavItem to="/upload">Upload</NavItem>
        <NavItem to="/account">Account</NavItem>
      </NavItems> */}
    </Nav>
  );
};

export default Navbar;
